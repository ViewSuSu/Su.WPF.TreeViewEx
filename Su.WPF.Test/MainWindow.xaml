<Window
    x:Class="Su.WPF.Test.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Su.WPF.Test"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:treeViewEx="clr-namespace:Su.WPF.CustomControl.TreeViewEx;assembly=Su.WPF.CustomControl"
    Title="MainWindow"
    Width="300"
    Height="600"
    d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
    Icon="{Binding Icon, Mode=OneWay}"
    Topmost="True"
    mc:Ignorable="d">
    <Grid>
        <ContentControl Content="{Binding Provider.TreeView, Mode=OneWay}" />
    </Grid>
</Window>
